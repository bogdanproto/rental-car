import styled from 'styled-components';

export const InputBoxIcon = styled.div`
  position: relative;

  input {
    padding-left: 24px;
  }

  svg {
    font-size: 12px;
    padding: 0;
    margin: 0;
    position: absolute;
    left: 8px;
    top: 50%;
    transform: translateY(-50%);
  }
`;
